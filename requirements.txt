# ChronoGate dependencies -- all permissive (BSD / MIT / PSF), no GPL.
#
# ptufile    BSD-3-Clause  -- reads PicoQuant .ptu TTTR files (the parser)
# numpy      BSD-3-Clause  -- the cube, prefix sum, all numerics
# matplotlib PSF/BSD-style -- the interactive GUI (SpanSelector + imshow)
# tifffile   BSD-3-Clause  -- writes the 16-bit raw TIFF export
#
# tkinter (for the file-open dialog) ships with Python; on Homebrew Python you
# may need `brew install python-tk`. It is OPTIONAL -- pass a file path instead.

ptufile>=2026.6.6
numpy>=1.24
matplotlib>=3.7
tifffile>=2023.7.10
